# 2026-09-15 worklog

**主题**：BUG-010 幽灵点击根治（SPA 化）+ 重测逐卡即时毕业
**时间窗**：00:10 – 00:4x（深夜速战，未碰 SQLite 大工程）
**Commit**：待补

---

## 一、BUG-010 破案：不是玄学，是 WebView 全页重载 + 连击穿透

**体感**：词义页点「下一词」没反应，再点直接越过新卡正面进词义页。

**根因链**：
1. 切卡 = `loadHtmlString(html)` 整页重载，Android 上 100~300ms 白屏/无响应。
2. 无响应期用户自然连击，第二击落点时新页恰好渲染完，正中底部「记得」按钮。
3. 模板脚本刚绑定事件就捕获这记幽灵点击 → `toMeaning("good")` → 正面被秒跳。

**修复（SPA 化）**：
- 骨架页（含模板 CSS+JS+glue）只在会话开始 `loadHtmlString` **一次**。
- 之后每切一张卡：原生 `runJavaScript("window.Flashcard.mountCard(json)")`，DOM 增量替换，无整页重载。
- `mountCard` 内 250ms **连点锁**（`clickLock`），彻底阻断幽灵点击。
- `_onMsg` 只在 `_ready`（onPageFinished）后才允许 `_load`，规避竞态。

**关键转义教训**：`json.encode` 输出已含 `\n` `\"` 等转义，嵌入 JS 单引号字符串时**只许再转义反斜杠与单引号**，加任何其他转义都会在 `JSON.parse` 后被二次反转（`\n` 变真换行、`\"` 变裸引号），直接炸 JSON。已用 Python 模拟链路验证无损往返。

---

## 二、重测逐卡即时毕业（消除连坐丢进度）

**旧行为**：重测轮只有整池「choice+cloze 全过」才批量毕业。池内 4 张已过、1 张死磕，中途强杀 = 已掌握卡全部丢进度。

**新行为**：`submitRetest` 中一张卡双过 → 立即 `graduated.add` + 滚出 `_retestPool`，`_onMsg` 随即落盘。已过的卡不再陪跑。

---

## 三、UI 深度大改版（参考图 1 与图 2 深度对标）

1. **Choice 选项卡片全面重做（对标图 1）**：
   - 4 个深色圆角大卡片（`#1b2327`，圆角 16px）。
   - 未选前：词性（`n.` / `vt.`）小胶囊 + 中文释义清晰大字。
   - 选错反馈（核心设计）：
     - 选错选项：暗红底色 + 红边框 + 红字，**上方直接揭晓对应的英文单词（如 `gourmet`）**！
     - 正确选项：墨绿底色 + 绿边框 + 绿字，**上方直接揭晓正确答案的英文单词（如 `outset`）**！
     - 其他未选选项淡化置灰。
   - 底部操作条：选完后高亮居中激活「继续」大按钮。

2. **词义页无限长页滚动（对标图 2）**：
   - 摒弃复杂标签栏，做成流畅的无限下滑长页。
   - 顶部：单词大字（支持音节切分 `out·set`）+ 美式音标胶囊 `美 🔊 /'aʊtset/`。
   - 核心释义：`n. 开始，起始`，底部浅虚线下划线。
   - 真题例句大卡片：英文关键词下划线高亮 + 中文翻译 + 朗读小按钮。
   - 常用短语大卡片 + 词根词源大卡片，内容全展示，下滑到底有充足留白。

3. **进入卡片自动播放发音 + 点击随时发音**：
   - 卡片 `mount()` 时自动触发系统 TTS 朗读单词。
   - 点击音标胶囊、单词本身、例句小喇叭随时朗读。
   - `_choicesFor` 数据结构追加干扰项的原本 `word` 字段。

---

## 四、改动清单

| 文件 | 改动 |
|---|---|
| `app/lib/screens/review_screen.dart` | 骨架页单次 load + `mountCard` 增量挂卡 + `_ready` 门锁 |
| `app/lib/services/webview_bridge.dart` | `buildCardPage` 只建骨架；新增 `mountCard(ctrl, ...)` |
| `app/lib/services/template_engine.dart` | glue JS 加 `mountCard`/`onMount`；`jsonForJs` 转义 |
| `templates/bubei_dark/script.js` | 改造为 SPA mount（含 250ms 连点锁）|
| `app/lib/models/study_session.dart` | 重测逐卡即时毕业 |
| `app/assets/templates/bubei_dark/` | 同步模板副本 |

---

## 四、待办（明天白天）

1. **SQLite 换血**（`CardStore` → sqflite，单行事务 + due 索引）—— P0
2. 干扰项同词性筛选 —— P1
3. 排课器 Planner —— P1
4. `webpreview` 接上 onMount 契约
5. 真机验证：连点穿透是否根治、重测强杀后进度是否保留
